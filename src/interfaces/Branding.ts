interface Branding {
  primaryColor: string;
  primaryHoverColor: string;
  iconStrokeWidth: number;
  borderRadius: number;
  boxShadowAlpha: number;
  animations: boolean;
}

export default Branding;
